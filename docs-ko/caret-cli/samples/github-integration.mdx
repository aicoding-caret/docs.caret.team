---
title: "GitHub Actions 통합"
description: "GitHub Actions에서 Caret CLI를 사용해 @caret 멘션 이슈에 자동 응답하기"
---

# GitHub 통합 샘플

AI로 GitHub 이슈 분석을 자동화하세요. 이슈 댓글에 `@caret`을 멘션하면 Caret이 파일을 읽고 코드를 분석해 실행 가능한 인사이트를 제공합니다. 모든 과정은 GitHub Actions에서 자동으로 수행됩니다.

<Note>
**Caret CLI가 처음이신가요?** 이 샘플은 Caret CLI 기본을 이해하고 [설치 가이드](/ko/caret-cli/installation)를 완료했다고 가정합니다. 처음이라면 더 간단한 [GitHub RCA 샘플](/ko/caret-cli/samples/github-issue-rca)부터 시작하는 것을 권장합니다.
</Note>

## 워크플로

이슈 댓글에서 `@caret`을 멘션하면 Caret가 트리거됩니다:

<Frame>
  <img src="https://storage.googleapis.com/cline_public_images/ss0a-comment.png" alt="Issue comment with @caret mention" width="600" />
</Frame>

Caret의 자동 분석 결과가 새 댓글로 게시됩니다:

<Frame>
  <img src="https://storage.googleapis.com/cline_public_images/ss0b-final.png" alt="Automated analysis response from Caret" width="600" />
</Frame>

이 전체 분석은 GitHub Actions에서 자율적으로 수행됩니다(파일 탐색 → 분석 → 결과 게시).

이제 저장소를 설정해 보겠습니다.

## 준비 사항

시작 전에 다음이 필요합니다:

- **Caret CLI 지식** - [설치 가이드](/ko/caret-cli/installation) 완료 및 기본 사용법 숙지
- **GitHub 저장소** - Actions와 시크릿을 설정할 관리자 권한
- **GitHub Actions 이해** - 워크플로/CI·CD 기본 개념
- **API 프로바이더 계정** - OpenRouter, Anthropic 등 API 키 보유

## 설정

### 1. 워크플로 파일 복사

샘플의 워크플로 파일을 저장소로 복사합니다. 워크플로 파일은 GitHub Actions가 인식하도록 저장소 루트의 `.github/workflows/`에 있어야 합니다. 여기서는 `caret-responder.yml`로 저장합니다.

```bash
# 저장소 루트에서
mkdir -p .github/workflows
curl -o .github/workflows/caret-responder.yml https://raw.githubusercontent.com/caret/caret/main/src/samples/cli/github-integration/caret-responder.yml
```

또는 `.github/workflows/caret-responder.yml`에 전체 내용을 직접 붙여 넣어도 됩니다:

<Accordion title="caret-responder.yml 전체 보기">
```yaml
name: Caret Issue Assistant

on:
  issue_comment:
    types: [created, edited]

permissions:
  issues: write

jobs:
  respond:
    runs-on: ubuntu-latest
    environment: caret-actions
    steps:
      - name: Check for @caret mention
        id: detect
        uses: actions/github-script@v7
        with:
          script: |
            const body = context.payload.comment?.body || "";
            const isPR = !!context.payload.issue?.pull_request;
            const hit = body.toLowerCase().includes("@caret");
            core.setOutput("hit", (!isPR && hit) ? "true" : "false");
            core.setOutput("issue_number", String(context.payload.issue?.number || ""));
            core.setOutput("issue_url", context.payload.issue?.html_url || "");
            core.setOutput("comment_body", body);

      - name: Checkout repository
        if: steps.detect.outputs.hit == 'true'
        uses: actions/checkout@v4

      # Node v20 is needed for Caret CLI on GitHub Actions Linux
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Caret CLI
        if: steps.detect.outputs.hit == 'true'
        run: |
          # Install the Caret CLI
          sudo npm install -g caret

      - name: Create Caret Instance
        if: steps.detect.outputs.hit == 'true'
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          CLINE_DIR: ${{ runner.temp }}/caret
        run: |
          # Create instance and capture output
          INSTANCE_OUTPUT=$(caret instance new 2>&1)
          
          # Parse address from output (format: "  Address: 127.0.0.1:36733")
          CLINE_ADDRESS=$(echo "$INSTANCE_OUTPUT" | grep "Address:" | grep -oE '([0-9]{1,3}\.){3}[0-9]{1,3}:[0-9]+')
          echo "CLINE_ADDRESS=$CLINE_ADDRESS" >> $GITHUB_ENV
          
          # Configure API key
          caret config set open-router-api-key=$OPENROUTER_API_KEY --address $CLINE_ADDRESS -v

      - name: Download analyze script
        if: steps.detect.outputs.hit == 'true'
        run: |
          export GITORG="YOUR-GITHUB-ORG"
          export GITREPO="YOUR-GITHUB-REPO"

          curl -L https://raw.githubusercontent.com/${GITORG}/${GITREPO}/refs/heads/main/git-scripts/analyze-issue.sh -o analyze-issue.sh
          chmod +x analyze-issue.sh

      - name: Run analysis
        if: steps.detect.outputs.hit == 'true'
        id: analyze
        env:
          ISSUE_URL: ${{ steps.detect.outputs.issue_url }}
          COMMENT: ${{ steps.detect.outputs.comment_body }}
          CLINE_ADDRESS: ${{ env.CLINE_ADDRESS }}
        run: |
          set -euo pipefail
          
          RESULT=$(./analyze-issue.sh "${ISSUE_URL}" "Analyze this issue. The user asked: ${COMMENT}" "$CLINE_ADDRESS")
          
          {
            echo 'result<<EOF'
            printf "%s\n" "$RESULT"
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      - name: Post response
        if: steps.detect.outputs.hit == 'true'
        uses: actions/github-script@v7
        env:
          ISSUE_NUMBER: ${{ steps.detect.outputs.issue_number }}
          RESULT: ${{ steps.analyze.outputs.result }}
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: Number(process.env.ISSUE_NUMBER),
              body: process.env.RESULT || "(no output)"
            });
```
</Accordion>

<Warning>
**커밋 전에 반드시 워크플로 파일을 수정하세요!**

`.github/workflows/caret-responder.yml`을 열고, 워크플로의 "Download analyze script" 단계에서 분석 스크립트를 저장한 GitHub 조직/저장소를 지정해야 합니다:

```yaml
export GITORG="YOUR-GITHUB-ORG"      # 여기를 변경하세요!
export GITREPO="YOUR-GITHUB-REPO"    # 여기를 변경하세요!
```

**예시:** 저장소가 `github.com/acme/myproject`라면 다음과 같이 설정합니다:
```yaml
export GITORG="acme"
export GITREPO="myproject"
```

이렇게 해야 3단계에서 커밋한 분석 스크립트를 워크플로가 올바른 위치에서 다운로드할 수 있습니다.
</Warning>

이 워크플로는 새로 생성되거나 수정된 이슈를 확인하고 `@caret` 멘션을 감지한 뒤, Caret CLI 인스턴스를 띄워 이슈를 분석하고 결과를 댓글로 남깁니다.

### 2. API 키 설정

AI 프로바이더 API 키를 저장소 시크릿에 추가합니다:

1. GitHub 저장소로 이동
2. **Settings** → **Environment**로 이동해 새 환경 추가

   <Frame>
     <img src="https://storage.googleapis.com/cline_public_images/ss01-environment.png" alt="Navigate to Actions secrets" width="600" />
   </Frame>

   `caret-responder.yml`의 `environment` 값과 일치하도록 이름을 "caret-actions"로 설정합니다.

3. **New repository secret** 클릭
4. [openrouter.com](https://openrouter.com)의 API 키로 `OPENROUTER_API_KEY` 시크릿 추가

   <Frame>
     <img src="https://storage.googleapis.com/cline_public_images/ss02-api-key.png" alt="Add API key secret" width="600" />
   </Frame>

5. 시크릿이 정상적으로 설정되었는지 확인

   <Frame>
     <img src="https://storage.googleapis.com/cline_public_images/ss03-ready.png" alt="API key configured" width="600" />
   </Frame>

이제 GitHub Action에서 Caret가 사용할 자격 증명을 제공할 준비가 끝났습니다.

### 3. 분석 스크립트 추가

`github-issue-rca` 샘플의 분석 스크립트를 저장소에 추가합니다. **먼저 저장소 루트에 `git-scripts` 디렉터리를 만들어 스크립트를 배치해야 합니다.** 아래 옵션 중 하나를 선택하세요.

**옵션 A: 직접 다운로드(권장)**

```bash
# 저장소 루트에서 디렉터리 생성 후 스크립트 다운로드
mkdir -p git-scripts
curl -o git-scripts/analyze-issue.sh https://raw.githubusercontent.com/caret/caret/main/src/samples/cli/github-issue-rca/analyze-issue.sh
chmod +x git-scripts/analyze-issue.sh
```

**옵션 B: 수동 복사/붙여넣기**

디렉터리와 파일을 직접 만든 뒤 내용을 붙여 넣습니다:

```bash
# 저장소 루트에서
mkdir -p git-scripts
# 원하는 에디터로 파일 생성/편집
nano git-scripts/analyze-issue.sh  # 또는 vim, code 등
```

<Accordion title="analyze-issue.sh 전체 보기">
```bash
#!/bin/bash
# Analyze a GitHub issue using Caret CLI

if [ -z "$1" ]; then
    echo "Usage: $0 <github-issue-url> [prompt] [address]"
    echo "Example: $0 https://github.com/owner/repo/issues/123"
    echo "Example: $0 https://github.com/owner/repo/issues/123 'What is the root cause of this issue?'"
    echo "Example: $0 https://github.com/owner/repo/issues/123 'What is the root cause of this issue?' 127.0.0.1:46529"
    exit 1
fi

# Gather the args
ISSUE_URL="$1"
PROMPT="${2:-What is the root cause of this issue?}"
if [ -n "$3" ]; then
    ADDRESS="--address $3"
fi

# Ask Caret for its analysis, showing only the summary
caret -y "$PROMPT: $ISSUE_URL" --mode act $ADDRESS -F json | \
    sed -n '/^{/,$p' | \
    jq -r 'select(.say == "completion_result") | .text' | \
    sed 's/\\n/\n/g'
```

After pasting the script content, make it executable:
```bash
chmod +x git-scripts/analyze-issue.sh
```
</Accordion>

이 분석 스크립트는 Caret에게 GitHub 이슈를 분석하도록 요청하고, 응답을 요약하여 댓글로 게시하는 데 필요한 출력만 추출합니다.

### 4. 커밋 및 푸시

```bash
git add .github/workflows/caret-responder.yml
git add git-scripts/analyze-issue.sh
git commit -m "Add Caret issue assistant workflow"
git push
```

## 사용 방법

설정이 끝나면 이슈 댓글에 `@caret`을 멘션하기만 하면 됩니다:

```
@caret what's causing this error?

@caret analyze the root cause

@caret what are the security implications?
```

GitHub Actions는 다음을 수행합니다:
1. `@caret` 멘션 감지
2. Caret CLI 인스턴스 생성
3. 분석 스크립트 다운로드
4. act 모드 + yolo로 이슈 분석
5. Caret 분석 결과를 댓글로 게시

**주의:** 이 워크플로는 이슈 댓글에서만 동작하며 PR 댓글에서는 동작하지 않습니다.

## 동작 원리

워크플로(`caret-responder.yml`)는 다음 단계로 동작합니다:

1. 이슈 댓글(생성/수정) 이벤트에 **트리거**
2. `@caret` 멘션을 **감지** (대소문자 무시)
3. npm으로 Caret CLI를 전역 **설치**
4. `caret instance new`로 Caret 인스턴스 **생성**
5. `caret config set open-router-api-key=... --address ...`로 **인증 구성**
6. `github-issue-rca` 샘플의 재사용 가능한 `analyze-issue.sh`를 **다운로드**
7. 인스턴스 주소로 **분석 실행**
8. 분석 결과를 댓글로 **게시**

## 관련 샘플

- **[github-issue-rca](./github-issue-rca)**: 이 통합의 핵심이 되는 재사용 가능한 분석 스크립트

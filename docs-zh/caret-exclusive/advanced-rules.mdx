---
title: 规则优先级系统
---

# 规则优先级系统

Caret 已将规则路径统一为 `.agents/context/` 单一路径，旧规则路径不再支持。

## 标准规则来源

- 工作区规则：`.agents/context/`
- 目录级指令：`AGENTS.md`（递归）
- 全局规则：Documents/Caret/Rules

## 变更原因

- 避免重复加载
- 消除规则冲突
- 规则加载可预测

## 迁移

将旧规则文件/目录移动到 `.agents/context/`。

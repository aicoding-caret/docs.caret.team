---
title: "Обзор"
description: "Установите CLI, запустите свою первую задачу и научитесь автоматизировать проверки кода и интегрировать AI-агентов в ваш процесс разработки"
---

<Warning>
**Preview Release - macOS and Linux Only**

Caret CLI в настоящее время находится на стадии предварительного просмотра и доступен только для пользователей macOS и Linux. Поддержка Windows появится в ближайшее время.
</Warning>

<Note>
이 매뉴얼은 Caret 기반으로 빌드된 캐럿 CLI를 기준으로 합니다. 기능·옵션은 Caret v3.38.1 머지본을 따르며, 캐럿 브랜딩/UI 차이는 최소화되어 있습니다.
</Note>

## Что такое Caret CLI?

Caret CLI запускает AI-агентов для кодирования непосредственно в вашем терминале. Передавайте `git diff` для автоматизированных проверок кода в CI/CD, запускайте несколько экземпляров одновременно для параллельной разработки или интегрируйте Caret в существующие рабочие процессы оболочки.

CLI отслеживает экземпляры в вашей системе и выводит данные в форматах, предназначенных как для людей, так и для скриптов — JSON, обычный текст или расширенный вывод в терминале.

<Tip>
Готовы начать? Ознакомьтесь с [руководством по установке](/russian/caret-cli/installation), чтобы установить Caret CLI и запустить свою первую задачу.
</Tip>

## Поддерживаемые провайдеры моделей

Caret CLI поддерживает несколько провайдеров AI-моделей, что дает вам гибкость в выборе лучшей модели для ваших нужд:

- **Anthropic**
- **OpenAI**
- **OpenAI Compatible**
- **OpenRouter**
- **X AI (Grok)**
- **AWS Bedrock**
- **Google Gemini**
- **Ollama**
- **Cerebras**

Во время установки вы пройдете аутентификацию и настроите предпочитаемого провайдера с помощью команды `caret auth`.

## Что вы можете создать с помощью этого

**Автоматизированное обслуживание кода**
- Запланируйте ежедневные запуски для выявления и исправления проблем линтинга во всей вашей кодовой базе
- Создавайте задачи, которые сканируют наличие уязвимостей безопасности и автоматически исправляют их
- Создавайте скрипты, которые обновляют устаревшие зависимости и запускают тесты

**Разработка с несколькими экземплярами**
- Запускайте отдельные экземпляры Caret для frontend и backend одновременно
- Создавайте экземпляры для разных веток feature, каждый с изолированным состоянием
- Создавайте параллельные процессы проверки для нескольких PR

**Пользовательские рабочие процессы**
- Создавайте shell-скрипты, которые объединяют Caret с `git hooks` для анализа перед коммитом
- Создавайте пользовательские команды, которые передают сложные структуры данных через Caret для обработки
- Интегрируйтесь с существующей цепочкой инструментов (jq, grep, awk) для сложной автоматизации

**Интеграция CI/CD**
- Добавьте Caret в GitHub Actions для автоматической проверки кода при каждом PR
- Создавайте конвейеры GitLab, которые генерируют скрипты миграции из изменений схемы
- Создавайте задания Jenkins, которые используют Caret для анализа сбоев тестов и предлагают исправления

## Узнать больше

<Columns cols={2}>
  <Card title="Установка" icon="download" href="/russian/caret-cli/installation">
    Установите Caret CLI и пройдите аутентификацию с вашей учетной записью, чтобы начать.
  </Card>
  
  <Card title="Три основных потока" icon="route" href="/russian/caret-cli/three-core-flows">
    Освойте три способа использования Caret CLI: интерактивный режим, автоматизация без графического интерфейса и параллелизация с несколькими экземплярами.
  </Card>
</Columns>
